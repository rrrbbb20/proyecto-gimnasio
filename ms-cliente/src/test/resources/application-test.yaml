spring:
  datasource:
    url: "jdbc:h2:mem:msautor_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE"
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate.format_sql: true
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false
jwt:
  secret: clave_super_secreta_12345678901234567890
logging:
  level:
    org.springframework.boot.autoconfigure.logging.ConditionEvaluationReportLogger: WARN